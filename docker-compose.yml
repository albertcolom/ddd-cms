version: '2'
services:
  mysql:
    image: mysql:5.7
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=ddd-cms
    volumes:
      - db_data:/var/lib/mysql
volumes:
  db_data:
